<% if @errors %>
	<%= render 'errors/errors', locals: {errors: @errors} %>
<% end %>

<%= form_for :user, url: '/login' do |f| %>
  <p>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :password %>
    <%= f.password_field :password %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>